# 업무 로그 2026-06-04 (야간 작업)

## 완료

### 1. APK 크래시 수정
- 원인: `expo-clipboard ^56.0.3` (SDK 54와 버전 충돌)
- 해결: `npx expo install --fix` 로 전체 expo 패키지 버전 정렬
- 결과: 앱 정상 실행 확인

### 2. app-redirect 라우트 추가
- 소셜 로그인 후 `instalink://app-redirect?code=...` 딥링크 수신 시 라우트 없어 에러 발생
- `app/app-redirect.tsx` 생성 → code 추출 후 `exchangeCodeForSession` 호출
- 성공 시 `/(app)`, 실패 시 `/(auth)/login` 리다이렉트

### 3. Supabase env 변수 EAS 빌드에 포함
- 원인: `.env.local`은 EAS 클라우드 빌드에 업로드 안 됨 → URL이 `undefined`로 빌드됨
- 해결: `eas.json`의 preview, development 프로필에 env 직접 추가

### 4. expo-dev-client 설치
- 패키지 설치 완료
- development 빌드 시작 (EAS 서버에서 진행 중)

## 진행 중

### development 빌드 대기 중
- EAS 빌드 URL: https://expo.dev/accounts/kku-studio/projects/instalink-app/builds/6e42f00a-08c2-4b2d-879c-7a09550bbf04
- 완료 후 설치 → `npx expo start` 로 dev-client 연결

## 현재 상태 (빌드 대기 중)
- 앱열기 버튼 → 앱으로 복귀는 동작함 (app-redirect 라우트 정상)
- 단, 로그인 시 계정 없다고 나옴 / 회원가입도 안 됨
- 원인: 현재 설치된 APK는 env 미포함 빌드 → Supabase URL이 undefined
- **새 빌드(development) 완료 후 설치하면 해결 예정**

## 내일 이어서 할 것
1. dev-client APK 빌드 완료 확인 후 설치
2. `npx expo start` 실행 → 핸드폰 QR 스캔으로 연결
3. 로그인 / 회원가입 / 소셜 로그인 테스트
4. 소셜 로그인 정상 동작 확인 후 추가 기능 개발
