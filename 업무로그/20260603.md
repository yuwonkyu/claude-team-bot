# 업무 로그 2026-06-03

## 완료

### 1. instalink-app 편집 화면 심화
- `app/(app)/edit.tsx` 서비스·후기·갤러리·커스텀 링크 4개 섹션 추가
- 항목 추가/삭제 가능한 배열 편집 UI 구현
- 추가 버튼 위치: 섹션 타이틀 옆 → 섹션 하단 점선 버튼 (UX 개선)
- 저장 시 빈 항목 자동 필터링

### 2. 소셜 로그인 코드 완성 (테스트 대기)
- 카카오(노란)/구글(흰) 버튼 UI 추가
- PKCE flow 적용 (`flowType: "pkce"` in supabase.ts)
- `redirectTo: "https://instalink.kkustudio.com/app-redirect"` (HTTPS 중간 페이지)
- `openAuthSessionAsync` + `exchangeCodeForSession` 흐름
- InstaLink 웹에 `/app-redirect` 중간 페이지 추가 (Vercel 배포 완료)
- Supabase Redirect URLs 등록 완료

**Expo Go 한계 확인:** `instalink://` 커스텀 scheme은 Expo Go 미지원 → EAS 빌드에서만 동작

### 3. EAS 빌드 세팅
- `eas build:configure` 완료, `eas.json` 생성
- `.npmrc` legacy-peer-deps 추가 (의존성 충돌 해결)
- EAS Secret 설정 (SUPABASE_URL, ANON_KEY) — git에 노출 없음
- preview APK 빌드 성공 및 설치 완료

## 미해결

### APK 크래시
- 설치 후 앱 실행 시 즉시 종료
- 원인 미파악 (글로벌 에러 핸들러 추가한 상태로 재빌드 대기)
- **집에서 ADB logcat으로 원인 확인 예정**

## 다음에 이어서 할 것
1. USB 디버깅으로 ADB 크래시 로그 확인
2. 크래시 수정 후 재빌드
3. 소셜 로그인 최종 테스트
4. expo-dev-client 빌드로 전환 (빠른 개발 사이클 구축)
5. 토스페이먼츠 연동 (6/20 심사 완료 후)
